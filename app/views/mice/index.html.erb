<p id="notice"><%= notice %></p>

<h1>Listing Mice</h1>

<table>
  <thead>
    <tr>
      <th>Dob</th>
      <th>Sex</th>
      <th>Number</th>
      <th>Genotype</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mice.each do |mouse| %>
      <tr>
        <td><%= mouse.dob %></td>
        <td><%= mouse.sex %></td>
        <td><%= mouse.number %></td>
        <td><%= mouse.genotype %></td>
        <td><%= link_to 'Show', mouse %></td>
      </tr>
    <% end %>
  </tbody>
</table>
