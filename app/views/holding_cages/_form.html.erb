<%= form_for(@holding_cage) do |f| %>
  <% if @holding_cage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holding_cage.errors.count, "error") %> prohibited this holding_cage from being saved:</h2>

      <ul>
      <% @holding_cage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cage_id %><br>
    <%= f.text_field :cage_id %>
  </div>
  <div class="field">
    <%= f.label :occupancy %><br>
    <%= f.number_field :occupancy %>
  </div>
  <div class="field">
    <%= f.label :sex %><br>
    <%= f.text_field :sex %>
  </div>
  <div class="field">
    <%= f.label :dob %><br>
    <%= f.date_select :dob %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
