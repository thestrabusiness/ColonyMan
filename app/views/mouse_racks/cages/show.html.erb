<p id="notice"><%= notice %></p>

<p>
  <strong>Cage:</strong>
  <%= @cage.cage_id %>
</p>

<p>
  <strong>Occupancy:</strong>
  <%= @cage.occupancy %>
</p>

<p>
  <strong>Sex:</strong>
  <%= @cage.sex %>
</p>

<p>
  <strong>Dob:</strong>
  <%= @cage.dob %>
</p>

<br>
<br>
<table>
  <thead>
    <tr>
      <th>Number</th>
      <th>Genotype</th>
      <th>DOB</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @mice.each do |mouse|%>
    <tr>
      <td><%= mouse.number %></td>
      <td><%= mouse.genotype %></td>
      <td><%= mouse.dob %></td>
      <td><%= link_to "Delete", [@cage, mouse], method: :delete, confirm: "Are you sure you want to remove this mouse from the cage?" %></td>
    </tr>
    <% end %>
  </tbody>
</table>
  

<%= link_to 'Add a mouse', new_cage_mouse_path(@cage, :sex => @cage.sex) %>
<br>
<br>
<%= link_to 'Edit', edit_mouse_rack_cage_path(@mouse_rack, @cage) %> |
<%= link_to 'Back', mouse_rack_path(@mouse_rack) %>
